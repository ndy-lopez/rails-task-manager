<h1><%= @task.title %></h1>
<h3><%= @task.details %></h3>

<h3><%= @task.completed ? '✅' : '' %></h3>

<p><%= link_to 'Go back to Tasks', tasks_path, class: "text-decoration-none" %><i class="fa-solid fa-backward"></i></p>

<p><%= link_to 'Edit',  edit_task_path(@task), class: "text-decoration-none" %><i class="fa-regular fa-pen-to-square"></i></p>

<p><%= link_to 'Delete',
       task_path(@task),
       data: { turbo_method: :delete, turbo_confirm: 'You sure?' }, class: "text-decoration-none" %><i class="fa-solid fa-trash"></i></p>
