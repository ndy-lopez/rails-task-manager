<h1>My Tasks</h1>


  <% @tasks.each do |task| %>
    <p><input type="checkbox"><%= link_to task.title, task_path(task), class: "text-decoration-none" %></p>
  <% end %>

<%= link_to 'Add a new task', tasks_new_path, class: "text-decoration-none btn btn-warning" %>
